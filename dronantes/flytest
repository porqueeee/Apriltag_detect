from djitellopy import Tello
import time

# Initialize and connect to the drone
tello = Tello()
tello.connect()
print(f"Battery: {tello.get_battery()}%")
# Take off
tello.takeoff()

time.sleep(2) 

# Move up 20 cm
tello.move_up(40)
time.sleep(2)
# Move forward 20 cm
#tello.move_forward(20)

# Rotate 90 degrees to the right (clockwise)
#tello.rotate_clockwise(90)

# Move forward 10 cm (in the new direction)
#tello.move_forward(10)

# Wait for 2 seconds
time.sleep(2)

# Land
tello.land()
